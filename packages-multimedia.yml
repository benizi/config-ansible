- name: Install many packages
  hosts: arch
  become: true
  tasks:
    - name: Multimedia
      pacman: name={{item}}
      with_items:
        - kodi
        - mencoder # split from `mplayer`
        - minidlna
        - mplayer
        - vlc
