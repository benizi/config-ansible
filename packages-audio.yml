- name: Install many packages
  hosts: arch
  become: true
  tasks:
    - name: Audio
      archpkg: name={{item}}
      with_items:
        - aur/spotify
        - festival-us
        - fluidsynth
        - soundfont-fluid
        - lilypond
        - pavucontrol
        - pulseaudio-alsa
        - sox
