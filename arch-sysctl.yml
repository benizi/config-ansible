- name: Arch sysctl compatibility
  hosts: arch
  sudo: yes
  tasks:
    - name: Link file to systemd location
      file: state=link
        src=/etc/sysctl.conf
        dest=/etc/sysctl.d/99-sysctl.conf
