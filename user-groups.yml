- name: Fix user groups
  hosts: arch
  tasks:
    - name: Permissions
      become: true
      user:
        name: bhaskell
        append: yes
        groups:
          - audio
          - systemd-journal
          - uucp
          - wireshark
